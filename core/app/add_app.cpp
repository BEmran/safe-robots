#include "core/add.hpp"

#include <iostream>

int main()
{ 
    std::cout << "Start app" << std::endl;

    // const std::vector<double> vec {1,2,3,4,5,6};
    // const auto [sum, mean] = core::accumulate_vector(vec);

    // std::cout << "vector sum = " << sum << ", mean = " << mean << std::endl;

    return 0;
}

